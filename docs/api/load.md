# load

Legacy module that mirrors `pipeline`. Use `pipeline` for new work.

## Key functions

- `metadata()`
- `field_list()`

## Example

```python
from phenofhy import load

meta = load.metadata()
```
