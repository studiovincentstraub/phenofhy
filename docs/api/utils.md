# utils

DNAnexus helpers and file utilities.

## Key functions

- `get_dataset_id()`
- `connect_to_dataset()`
- `find_latest_dx_file_id()`
- `download_files()` and `upload_files()`
- `load_file()`

## Example

```python
from phenofhy import utils

dataset_id = utils.get_dataset_id()
```
